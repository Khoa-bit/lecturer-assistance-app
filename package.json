{
  "name": "lecturer-assistance-app",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pn nx run gami:build && pn nx run raito:build"
  },
  "private": true,
  "devDependencies": {
    "nx": "16.0.3",
    "nx-cloud": "16.0.5",
    "prettier": "^2.8.8"
  }
}
