{
  "name": "raito",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "go run main.go serve",
    "serve": "./raito-pocketbase serve",
    "build": "go build",
    "build:types": "pocketbase-typegen --db ./pb_data/data.db --out ./types/pocketbase-types.ts && ./installToGami.sh",
    "debug:build": "go build -gcflags='all=-N -l' -o myApp"
  },
  "devDependencies": {
    "pocketbase-typegen": "^1.1.9",
    "typescript": "^5.0.4"
  }
}
